import { css } from "@styled-system/css";

export const container = css({
    padding: '18px',
    // paddingTop: '22px',
    paddingTop: '14px',
    paddingBottom: '10px'
});

export const urlSelector = css({
    marginBottom: '15px',
});

export const tabs = css({
    // marginBottom: '5px',
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center'
});

export const left = css({
    height: '100%',
    display: 'flex',
    flexDir: 'column',
});