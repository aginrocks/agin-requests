import { css } from "@styled-system/css";

export const right = css({
    height: '100%',
    display: 'flex',
    flexDir: 'column',
});

export const container = css({
    flex: 1,
    display: 'flex',
    flexDir: 'column',
    height: '100%',
});